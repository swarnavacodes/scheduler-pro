// Copy to supabase.config.js and fill — supabase.config.js is gitignored
// Get these from Supabase Dashboard → Project Settings → API
export const SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY_HERE';

// Optional: set to true to disable sync and force localStorage-only (for debugging)
export const DISABLE_SYNC = false;
